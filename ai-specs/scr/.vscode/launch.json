{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "coreclr",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}